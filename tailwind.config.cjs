const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {
      fontFamily: {
        sans: ['Alpino', 'Kola', 'Figtree']
      }
    }
  },

  plugins: []
};

module.exports = config;
