const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {
      fontFamily: {
        sans: ['Whirly Birdie']
      }
    }
  },

  plugins: []
};

module.exports = config;
